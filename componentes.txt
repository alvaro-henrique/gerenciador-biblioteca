Grupo:

Componente 1: NOME - MATRICULA
Componente 2: NOME - MATRICULA
Componente 3: NOME - MATRICULA
Componente 4: NOME - MATRICULA

Link do repositorio GitHub:
https://github.com/usuario/repositorio

Instrucoes para rodar:
1. Abrir o terminal na pasta do projeto.
2. Compilar no PowerShell:
   $dest = "out"
   New-Item -ItemType Directory -Force -Path $dest | Out-Null
   $sources = Get-ChildItem -Recurse src -Filter *.java | ForEach-Object { $_.FullName }
   javac -encoding UTF-8 -d $dest $sources
3. Executar:
   java -cp out br.com.biblioteca.Main
